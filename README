This directory contains the definition files for the syntax of languages used in PFPL.

The compilation parameters are in params.tex.

The definitions of general usage are found in
    1. Broad class of notations: generic.tex
    2. Language names: lang.tex
    3. Abstract syntax: syn.tex
    4. Judgments: jdg.tex
    5. Transition systems: ts.tex

The file defns-base.tex loads the compilation parameters and the general-use definitions.

The file defns-chaps.tex loads the base and all chapters.  For fewer chapters use your own defns-chaps file.
